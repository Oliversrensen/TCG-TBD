# WebSocket URL for the TCG game server.
# Copy this file to .env and set your URL.
#
# Local server (default if unset):
# VITE_TCG_SERVER=ws://localhost:8765
#
# Render (replace with your service URL):
# VITE_TCG_SERVER=wss://tcg-server-xxxx.onrender.com

VITE_TCG_SERVER=ws://localhost:8765

# Neon Auth base URL (from Neon Console → Auth → Configuration).
# Required for sign-in/sign-up in the web client.
# VITE_NEON_AUTH_URL=https://ep-xxx.neonauth.us-east-2.aws.neon.build/neondb/auth
VITE_NEON_AUTH_URL=
